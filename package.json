{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "verify": "pnpm build && pnpm lint && pnpm test",
    "publish-please": "pnpm verify && pnpm --filter use-trace publish"
  }
}
